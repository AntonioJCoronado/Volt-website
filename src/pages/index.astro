---
import MainLayout from "../layouts/MainLayout.astro";
---

<MainLayout
  title="Inicio"
  description="VOLT - Tu proveedor confiable de tecnología y accesorios para revendedores. Productos y precios Mayorista Volt. Ventas por Mayor. Importadores"
  currentPage="inicio"
>
  <section class="hero">
    <div class="bubbles">
      <div class="bubble"></div>
      <div class="bubble"></div>
      <div class="bubble"></div>
      <div class="bubble"></div>
      <div class="bubble"></div>
      <div class="bubble"></div>
    </div>

    <div class="container">
      <div class="content">
        <h1 class="title">
          <span class="title-dark">PRECIOS MAYORISTAS</span>
          <span class="title-blue">IMPORTADOR</span>
        </h1>
        <p class="description">TECNOLOGÍA DE CALIDAD PARA POTENCIAR TU NEGOCIO</p>
        <a href="/Volt-website/productos" class="btn">VER CATÁLOGO</a>
        <div class="mayorista-badges">
          <div class="badge">
            <span class="icon">📦</span>
            <span class="text">Compras superiores a $500.000</span>
          </div>
          <div class="badge">
            <span class="icon">💰</span>
            <span class="text">Los precios más competitivos del mercado</span>
          </div>
          <div class="badge">
            <span class="icon">🔑</span>
            <span class="text">Asesoramiento personalizado para revendedores</span>
          </div>
        </div>
      </div>

      <div class="image-container">
        <img 
          src="/Volt-website/images/logos/logo-volt.webp"
          alt="Logo VOLT Mayorista" 
          id="animated-logo"
          width="300"
          height="300"
        />
      </div>
    </div>
  </section>

  <script>
    document.addEventListener("DOMContentLoaded", function () {
      const logo = document.getElementById("animated-logo");

      if (!logo) {
        return;
      }

      let angle = 0;

      function animateLogo() {
        angle += 0.01;

        const moveX = Math.sin(angle) * 10;
        const moveY = Math.cos(angle) * 10;

        // @ts-ignore
        logo.style.transform = `translate(${moveX}px, ${moveY}px)`;

        requestAnimationFrame(animateLogo);
      }

      animateLogo();

      logo.addEventListener("mouseover", function () {
        this.style.filter = "drop-shadow(0 0 10px rgba(33, 150, 243, 0.6))";
      });

      logo.addEventListener("mouseout", function () {
        this.style.filter = "none";
      });
    });
  </script>
</MainLayout>
